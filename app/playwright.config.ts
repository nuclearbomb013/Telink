import { defineConfig, devices } from '@playwright/test';

/**
 * E2E test configuration.
 *
 * NOTE: Currently only starts the frontend dev server (npm run dev).
 * Full E2E tests require the backend server to be running separately.
 * Future enhancement: add backend startup + DB seed step for complete end-to-end testing.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
  },
});
